# Get it

    git clone --recursive https://github.com/7PintsOfCherryGarcia/squeezma.git

# Compile it

    cd squeezma
    make

# Run it

    //Compress
    sqz <input>

    //Decompress
    sqz -d <input>

# Link it

    //Static (Don't forget zlib)
    gcc yourcode.c libsqz.a -lz

    //Dynamic (Assuming your linker can find libsqz.so, don't forget zlib)
    gcc yourcode.c -lsqz -lz



####################IGNORE#################################################3
sqz format changed from mobile

A file compressed with sqz follows the following format:

    1 [HEADER]   |8 bytes|
    N [CMPBLOCK] |M bytes|
    1 [TAIL]     |  bytes|

HEADER format - [magic number: 5859]               |4 bytes|
                [sequence format: 0-fasta, 1-fastq]|1 bytes|
                [compression API: 1-zlib]          |1 bytes|
                [zeroes: 00]                       |2 bytes|

CMPBLOCK format - [decompressed size]                |8 bytes|
                  [compressed size]                  |8 bytes|
                   [compressed data block]            |compressed size bytes|
